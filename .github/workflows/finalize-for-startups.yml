name: Finalize For Startups Frontend

on:
  pull_request:
    branches: [main]
    types: [opened]

permissions:
  contents: write

jobs:
  finalize:
    if: github.head_ref == 'feat/for-startups'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/for-startups
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Restore canonical page source
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p src/startups public/assets/codia
          cat .tmp/for-startups/source.*.b64 | base64 -d | gzip -d > src/startups/MahForStartups.tsx
      - name: Localize Codia assets
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            curl -fsSL "https://static.codia.ai/image/2026-08-22/$f" -o "public/assets/codia/$f"
          done < .tmp/for-startups/assets.txt
      - name: Install and verify
        shell: bash
        run: |
          set +e
          npm install >/tmp/install.log 2>&1
          install_code=$?
          if [ "$install_code" -eq 0 ]; then
            npm run build >/tmp/build.log 2>&1
            build_code=$?
          else
            build_code=99
          fi
          set -e
          remote_refs=$(grep -R 'static.codia.ai' src index.html --exclude-dir=node_modules | wc -l || true)
          asset_count=$(find public/assets/codia -type f | wc -l)
          echo "install=$install_code"
          echo "build=$build_code"
          echo "asset_count=$asset_count"
          echo "expected_assets=53"
          echo "remote_codia_refs=$remote_refs"
          if [ "$install_code" -ne 0 ]; then cat /tmp/install.log; fi
          if [ "$build_code" -ne 0 ] && [ -f /tmp/build.log ]; then cat /tmp/build.log; fi
          test "$install_code" -eq 0
          test "$build_code" -eq 0
          test "$remote_refs" -eq 0
          test "$asset_count" -eq 53
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/startups/MahForStartups.tsx public/assets/codia package-lock.json
          git commit -m "feat(startups): finalize page with local assets" || true
          git push origin HEAD:feat/for-startups
