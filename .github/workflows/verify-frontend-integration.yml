name: Verify frontend integration

on:
  push:
    branches:
      - integration/frontend-foundation
      - integration/landing-frontend
      - feat/startup-panel
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: frontend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify startup panel routes
        run: |
          grep -Fq 'const StartupPanelDashboardPage = lazy(() => import("../features/startup-panel"));' src/app/router.tsx
          grep -Fq 'const StartupPanelProjectsPage = lazy(() => import("../features/startup-panel/projects"));' src/app/router.tsx
          test "$(grep -Fc '<Route path="/panel/startup" element={startupPanelDashboardElement} />' src/app/router.tsx)" -eq 1
          test "$(grep -Fc '<Route path="/panel/startup/projects" element={startupPanelProjectsElement} />' src/app/router.tsx)" -eq 1

      - name: Verify local Figma assets
        run: |
          test -f public/assets/startup-panel/logo.png
          test -f public/assets/startup-panel/lifecycle-active.svg
          test -f public/assets/startup-panel/lifecycle-default.svg
          test -f src/features/startup-panel/components/StartupSidebar.tsx
          test -f src/features/startup-panel/projects/index.tsx
          if grep -R "figma.com/api/mcp/asset\|static.codia.ai" src/features/startup-panel public/assets/startup-panel; then
            echo "Remote design asset URL detected"
            exit 1
          fi

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Build
        run: npm run build

      - name: Verify lockfile unchanged
        run: git diff --exit-code -- package-lock.json
